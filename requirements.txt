# Direct dependencies for the QEM pilot study.
# Exact transitive pins are in requirements-lock.txt (generated by `pip freeze`).
# Python 3.12.3 (CPython, Linux x86_64)
qiskit==2.5.2
qiskit-aer==0.17.2
qiskit-ibm-runtime==0.42.0
mitiq==1.1.0
numpy==2.2.6
scipy==1.17.1
pandas>=2.2
matplotlib>=3.9
networkx>=3.3
PyYAML>=6.0
# Required transitively by mitiq's Qiskit<->Cirq conversion (cirq.contrib.qasm_import).
# NOT pulled in automatically by `pip install mitiq`. Without it, mitiq.zne.scaling.fold_global
# raises ModuleNotFoundError: No module named 'ply' and the entire ZNE arm cannot run.
ply>=3.11
